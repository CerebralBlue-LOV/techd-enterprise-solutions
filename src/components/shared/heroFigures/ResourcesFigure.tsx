/**
 * ResourcesFigure — placeholder. The resources hero motif has not been
 * designed yet; this slot is rendered in /figure-lab so we can iterate on it.
 */
export const ResourcesFigure = () => (
  <div className="absolute inset-0 flex items-center justify-center">
    <div className="rounded-md border border-dashed border-border px-4 py-2 text-xs uppercase tracking-[0.2em] text-muted-foreground">
      Resources figure — TBD
    </div>
  </div>
);

export default ResourcesFigure;
