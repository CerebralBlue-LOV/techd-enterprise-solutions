## Goal

Promote the four lab-validated upscaled images to the production brand assets, replace every consumer (header, footer, intro, chat, favicon, debug inspectors), and remove the now-orphaned old files.

## Asset mapping

| New (source) | Replaces (destination) | Consumers |
|---|---|---|
| `src/assets/brand/lab/techd-logo-upscale.png` | `src/assets/brand/techd-logo.png` (new, replaces `techd-logo.webp`) | Header, Footer, LockupAlignmentInspector, favicon script |
| `src/assets/brand/lab/techd-wordmark-upscale.png` | `src/assets/brand/techd-wordmark.png` (overwrite) | IntroSplash |
| `src/assets/brand/lab/techd-gear-upscale.png` | `src/assets/brand/techd-gear.png` (overwrite) | IntroSplash |
| `src/assets/brand/lab/techd-gear-upscale-white.png` | `src/assets/brand/techd-mark-white.png` (overwrite) | ChatLauncher, ChatMessage, ChatPanel |

Also replace the debug copies under `public/logos/`:
- `public/logos/techd-gear.png` ← new gear upscale
- `public/logos/techd-wordmark.png` ← new wordmark upscale

## Steps

### 1. Copy lab files into production paths
- Copy `lab/techd-logo-upscale.png` → `src/assets/brand/techd-logo.png`
- Overwrite `src/assets/brand/techd-wordmark.png` from `lab/techd-wordmark-upscale.png`
- Overwrite `src/assets/brand/techd-gear.png` from `lab/techd-gear-upscale.png`
- Overwrite `src/assets/brand/techd-mark-white.png` from `lab/techd-gear-upscale-white.png`
- Overwrite `public/logos/techd-gear.png` and `public/logos/techd-wordmark.png` from the new gear and wordmark

### 2. Switch full-logo extension from `.webp` to `.png`
Update import statements in three components and one script:
- `src/components/layout/Header.tsx` → `import logo from "@/assets/brand/techd-logo.png"`
- `src/components/layout/Footer.tsx` → same
- `src/components/shared/LockupAlignmentInspector.tsx` → same (plus update the on-screen label `Original · techd-logo.webp` → `.png`)
- `scripts/generate-favicon.py` → change `SRC_LOGO` from `techd-logo.webp` to `techd-logo.png`

### 3. Regenerate the favicon
Run `python3 scripts/generate-favicon.py` so `public/favicon.ico`, `favicon-16x16.png`, `favicon-32x32.png`, and `apple-touch-icon.png` are rebuilt from the new high-resolution logo. The script already writes them to `public/` and updates `index.html`.

### 4. Delete the now-orphaned old logo
- `src/assets/brand/techd-logo.webp` (no longer imported anywhere)

### 5. Verify
- TypeScript build runs clean (the harness handles this).
- Visually spot-check the preview: Header, Footer, intro splash (gear + wordmark animation), the chat launcher button (white mark on cyan), and the favicon tab icon.

## Files touched

**Replaced / overwritten:**
- `src/assets/brand/techd-wordmark.png`
- `src/assets/brand/techd-gear.png`
- `src/assets/brand/techd-mark-white.png`
- `public/logos/techd-gear.png`
- `public/logos/techd-wordmark.png`
- `public/favicon.ico`, `favicon-16x16.png`, `favicon-32x32.png`, `apple-touch-icon.png` (regenerated by script)

**Created:**
- `src/assets/brand/techd-logo.png`

**Deleted:**
- `src/assets/brand/techd-logo.webp`

**Edited:**
- `src/components/layout/Header.tsx` (import path)
- `src/components/layout/Footer.tsx` (import path)
- `src/components/shared/LockupAlignmentInspector.tsx` (import path + label text)
- `scripts/generate-favicon.py` (source path)

## Out of scope (per your answers)

- `src/assets/brand/lab/` stays. The TechD Brand Lab page (`/techd-brand-lab`) keeps working as-is for future asset comparisons.
- No changes to `scripts/generate-techd-mark-white.py` (still useful if we re-derive the white mark from the gear later).
- No content, route, or component changes beyond the import/label tweaks listed.

## Risks / things to double-check after switching

- The new full logo is ~1264×848 (vs 264×70 old). Header/Footer render it via CSS `height` — those `<img>` tags should display fine, but I'll spot-check that the header lockup doesn't visually shift due to slightly different aspect ratio or letter-spacing.
- The new gear PNG has internal transparency between the teeth, so anywhere it's placed on a non-white background (intro splash, chat) it should still look right — the chat already uses the white variant, so this only affects IntroSplash which renders on a white background.
